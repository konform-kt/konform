def pomConfig = {
    licenses {
        license {
            name "The MIT License"
            url "http://opensource.org/licenses/MIT"
            distribution "repo"
        }
    }
    developers {
        developer {
            id projectDevelNick
            name projectDevelName
        }
    }

    scm {
        url "https://github.com/konform-kt/konform.git"
    }
}

def withPom = { pom ->
    pom.withXml {
        def root = asNode()
        root.appendNode('description', projectDesc)
        root.appendNode('url', 'https://github.com/konform-kt/konform')
        root.children().last() + pomConfig
    }
}

ext.withPom = withPom
